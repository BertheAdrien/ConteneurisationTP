apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000 # Port exposé à l'intérieur du cluster
      targetPort: 5000
  type: ClusterIP # Invisible de l'extérieur, accessible par le Frontend